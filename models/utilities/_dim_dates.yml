version: 2

models:
  - name: dim_dates
    description: Calendar date dimension derived from the daily time spine.
    columns:
      - name: date_day
        tests:
          - unique
          - not_null
      - name: date_month
        tests:
          - not_null
